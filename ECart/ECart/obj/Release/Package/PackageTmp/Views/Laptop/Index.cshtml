@model IEnumerable<ECart.ViewModels.LaptopViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LapCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LapCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LapCategoryId })
            @Html.ActionLink("Details", "Details", new { id=item.LapCategoryId }) 
            @Html.ActionLink("Delete", "Delete", new { id=item.LapCategoryId })
        </td>
    </tr>
}

</table>
